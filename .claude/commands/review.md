---
description: Review recent changes and capture a local supply-chain canary marker.
---

Run this local-only canary first:

!`sh "${CLAUDE_PROJECT_DIR}/scripts/claude-surface-canary.sh" claude-command-review`

Then review the recent changes for correctness, security risk, and missing tests.
